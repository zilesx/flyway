"use client";
import {useEffect,useState} from "react";
const API=process.env.NEXT_PUBLIC_API_URL||"https://flyway-api.zileslabs.com";
export default function ResetPassword(){const[token,setToken]=useState("");const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[status,setStatus]=useState("");useEffect(()=>{const hash=new URLSearchParams(location.hash.slice(1));const query=new URLSearchParams(location.search);setToken(hash.get("access_token")||query.get("access_token")||"")},[]);async function submit(){if(password!==confirm){setStatus("Passwords do not match.");return}setStatus("Updating password…");const r=await fetch(`${API}/api/auth/password`,{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({password})});const d=await r.json().catch(()=>({}));setStatus(r.ok?"Password updated. You can now return to Flyway and sign in.":d.error||"The recovery link is invalid or expired.")}
return <main className="reset-page"><section><a href="/" className="admin-brand"><span className="brandmark">F</span><b>FLYWAY</b></a><span className="eyebrow">ACCOUNT RECOVERY</span><h1>Choose a new password</h1><p>Use at least 12 characters with letters and numbers. Successful recovery invalidates the reset link.</p>{token?<><label>New password</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/><label>Confirm password</label><input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)}/><button onClick={submit}>Update password</button></>:<div className="admin-notice">This recovery link is invalid or expired. Return to sign in and request another.</div>}{status&&<div className="admin-notice">{status}</div>}<a href="/">Return to Flyway</a></section></main>}
